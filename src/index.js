import React from 'react';
import ReactDOM from 'react-dom/client';

const App = () => <h1>Brain Tasks App is running!</h1>;

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
